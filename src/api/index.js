// import axios from "axios";

// const baseUrl = "http://localhost:3000";

// // Create an instance of axios with the base URL
// const callersService = axios.create({
//   baseUrl,
// });

// // Fetch all callers
// export const fetchCallers = () => callersService.get("/callers");

// // Create a new caller
// export const createCaller = newCaller =>
//   callersService.post("/callers", newCaller);

// // Update a caller by ID
// export const updateCaller = (id, updatedCaller) =>
//   callersService.patch(`/callers/${id}`, updatedCaller);

// // Delete a caller by ID
// export const deleteCaller = id => callersService.delete(`/callers/${id}`);
