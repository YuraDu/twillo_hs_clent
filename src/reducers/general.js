import { createSlice } from "@reduxjs/toolkit";

const initialState = {

};

const general = createSlice({
  name: "counter",
  initialState,
  reducers: {

  },
});

// export const {
// } = general.actions;

export default general.reducer;
